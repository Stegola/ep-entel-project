<h1>Employees</h1>

<table class="table table-striped">
   <tr>
    <th>Name</th>
    <th>Surname</th>
  </tr>
  
  <% @employees.each do |employee| %>
    <tr>
      <td><%= employee.name %></td>
      <td><%= employee.surname %></td>
    </tr>
  <% end %>
</table>